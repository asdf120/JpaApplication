spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      # 애플리케이션 실행 시점에 테이블 생성, 가지고 있는 테이블을 지우고 엔티티 정보를 보고 다시생성
#      ddl-auto: create
#    properties:
#      hibernate:
#        #show_sql: true #sout에서 출력
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace